# 'ignoring' and 'on' keywords

# not working
# prometheus_http_requests_total and ignoring(handler) promhttp_metric_handler_errors_total
# promhttp_metric_handler_errors_total and on(code) prometheus_http_requests_total
