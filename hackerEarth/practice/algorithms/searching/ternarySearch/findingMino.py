# Finding Mino

#######################################################################################################################
#
#   You have been given an Unimodal function:
#   f(x) = 2x^2 − 12x + 7
#   with N intervals. For each interval you will be given two integer values l and r , where l≤r and you need to
#   find the minimum value of f(x) where x will be in the range [l,r] (both inclusive).
#
#   Input:
#   The first line will consists of one integer N
#   denoting the number of intervals.
#   In next N lines, each line contains 2 space separated integers, l and r denoting the range of interval.
#
#   Output:
#   Print N lines, where ith line denotes the minimum value of f(x), where x will be in range [li,ri].
#
#   Constraints:
#   1 <= N <= 10^5
#   −10^6 <= l <= r <= 10^6
#
#   SAMPLE INPUT
#   1
#   6 8
#
#   SAMPLE OUTPUT
#   7
#
#######################################################################################################################
