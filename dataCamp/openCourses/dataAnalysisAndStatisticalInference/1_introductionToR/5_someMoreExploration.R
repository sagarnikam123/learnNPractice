# Some more exploration

#######################################################################################################################
#
#	You are doing really well! Let's keep it up!
#
#	At this point, you might notice that many of the commands in R look a lot like functions from math class;
#	that is, invoking R commands means supplying a function with some number of arguments.
#
#	Let's start to examine the data a little more closely. You can access the data in a single column of a data
#	frame separately using a command like my_data_frame$variable_name.
#
#######################################################################################################################

# The present dataset is already loaded

# Find the number of boys born each year
num_boys <- present$boys

# Find the number of girls born each year
num_girls <- present$girls

# This will print the results
num_boys
num_girls
