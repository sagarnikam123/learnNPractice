# assigning values to variables
a <- 1
b <- 1
c <- -1

# view values
a
print(a)
ls() # name of objects save in your workspace

# solving the equation  x2+x−1=0
# solving the quadratic equation
(-b + sqrt(b^2 - 4*a*c)) / (2*a)
(-b - sqrt(b^2 - 4*a*c)) / (2*a)
