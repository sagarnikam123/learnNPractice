# check webserver image
docker images

# running container
docker run -d -p 80:80 webserver

#chekcing container running state
docker ps

# open browser & type
localhost

# stop container
docker stop small_easley
# or
docker stop webserver
