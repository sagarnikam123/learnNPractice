# 4_Launching the consul-server Node

# vagrant up --provider { parallels | virtualbox | vmware_desktop }
vagrant up --provider virtualbox (# to start/restart)

# ssh to vagrant machine
vagrant ssh consul-server

# suspend vagrant machine
vagrant suspend

# shutdown vagrant machine
vagrant halt # to shut down
